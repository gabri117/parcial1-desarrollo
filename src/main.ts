import './styles.css'
import { db } from './data/db'

// El renderizado se implementará más adelante.
// Por ahora solo verificamos que los datos se importan correctamente.
console.log(`GuitarLA — ${db.length} guitarras cargadas`)
